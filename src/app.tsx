import * as React from 'react';
import * as ReactDOM from 'react-dom';

const App = () => {
    return (
        <h1>App</h1>
    )
};

ReactDOM.render(
    <App />,
    document.getElementById('app')
);
